# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
.terraform.lock.hcl

# Lambda
lambda/node_modules/
lambda/function.zip

# Lambda@Edge (rendered by Terraform — contains secrets)
lambda-edge/index.js
lambda-edge/function.zip
